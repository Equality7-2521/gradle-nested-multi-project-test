plugins {
    id 'application'
}

dependencies {
    implementation(project(':service:one:impl'))
    implementation(project(':service:two:impl'))
}

application {
    mainClassName = 'foobarbaz.server.Main'
}
