plugins {
    id 'application'
}

dependencies {
    implementation(project(':service:one:impl'))
    implementation(project(':service:two:impl2'))
}

application {
    mainClassName = 'foobarbaz.server.Main'
}
